## ToDo currently / Priority indicator: H - High, M - Medium, L - Low, NaN - Not available

- [x] Sometimes it seems to be stopping, after it does, context gets reset or lowered. Might be because since it stops, it forgets everything in that duration and resets the context to last completed chat. (H)

- [x] Max tool calls need to be removed, it can call as much as it needs instead of 20. (M)

- [x] Auto compact needed if we haven't got one already, needs to receive a clean summarization of the previous context. (H)

- [x] Stop resetting the selected model, context length or selected settings when a new chat initiates. (L)

- [x] Adding web fetching tool, possibly using duckduckgo? (M)

- [x] Allow thinking, and asking questions if needing further confirmation (H)

- [x] Let LLM create/remove goals and see them. Also display it under the chat. (M)

- [x] Show the running cmd/powershell under the chat and allow stopping or pausing them (L)

### ToDo later the tests and fixes

- Workflow and subagents. (NaN)

